[project]
name = "stealth-dos-simulator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "docker>=7.1.0",
    "locust>=2.44.0",
    "seaborn>=0.13.2",
]

[tool.uv.workspace]
members = [
    "target_env",
    "ml_pipeline",
    "traffic/background",
    "traffic/manager",
]
